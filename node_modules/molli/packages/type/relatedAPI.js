const SimpleSchema = require('simpl-schema').default
const Kind = require('../kind')

class RelatedAPI {
  constructor(config) {
    const ConfigShape = new SimpleSchema({
      name: String,
      type: String,
    })
    ConfigShape.validate(config)
    Object.assign(
      this,
      { kind: Kind.RELATED_API },
      config
    )
  }
}

module.exports = RelatedAPI
