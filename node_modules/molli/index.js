#!/usr/bin/env node
const program = require('commander')
const { Hub, Event, Logger } = require('./packages')
program
  .version('0.0.1')
  .parse(process.argv)
Logger.watermark()
Hub.emit(Event.STARTUP)
